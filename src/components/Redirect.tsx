import * as React from 'react';


export interface IRedirectProps { };


const Redirect = React.memo<IRedirectProps>((
  props: IRedirectProps,
): JSX.Element => {
  return (
    <React.Fragment />
  );
});


export default Redirect;