import * as React from 'react';


export interface ILinkProps { };


const Link = React.memo<ILinkProps>((
  props: ILinkProps,
): JSX.Element => {
  return (
    <React.Fragment />
  );
});


export default Link;